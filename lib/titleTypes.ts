// ★홈판 제목 유형 8종 + 적합도 선택(2026-08-03 논쟁형 추가)(2026-08-02 유저 확정 — "그 글에 핏한 걸 선택해서 작성").
//
//  왜 새로 만드는가: 기존 훅 배정(hookPatterns.pickHookPattern, homefeedBet의 dayIdx 회전)은 전부
//  '날짜·유저 해시 추첨'이었다. 추첨은 반복을 막을 뿐 글감에 맞는 유형을 고르지 못한다 —
//  마감이 있는 글감에 '비교형'이, 비교축이 없는 글감에 '비교형'이 걸리면 제목이 본문을 배신한다.
//  그래서 글감 텍스트에서 신호를 읽어 적합도를 매기고, 최고점을 고른다. 추첨은 동점일 때만 쓴다.
//
//  ★유형은 '문체'가 아니라 '약속'이다. 제목이 무엇을 약속하느냐가 유형을 결정하고,
//  본문은 그 약속을 이행해야 한다(미이행 = 낚시 = 체류 붕괴 = 다음 노출 사망).

export type TitleTypeKey = "gain" | "threat" | "curious" | "compare" | "twist" | "action" | "experience" | "debate";

export interface TitleType {
  key: TitleTypeKey;
  name: string;
  /** LLM에게 주는 작성 지침 — 이 유형의 '약속'이 무엇인지. */
  guide: string;
  /** 본문이 반드시 이행해야 할 것 — 제목이 약속했으면 본문에 있어야 한다. */
  payoff: string;
  example: string;
}

// ★모든 유형이 공유하는 '문장 모양'(2026-08-03 — 유저 제공 상위 경제 블로그 제목 44개 실측).
//  유형은 '무엇을 약속하느냐'를 정하고, 이 규칙은 '어떻게 끝맺느냐'를 정한다 — 둘은 직교한다.
//  ★실측에서 가장 뚜렷했던 차이: 상위 제목은 문장으로 완결하지 않는다.
//   우리 종전 예시는 전부 '~습니다'로 닫혔다('...이유가 있습니다', '...이 셋이면 됩니다').
//   닫힌 문장은 그 자체로 답처럼 읽혀서 들어올 이유가 줄어든다. 상위 제목은 명사구나 전언형으로 끊는다.
//  ★홈판 keyword는 검색어가 아니라 주제 앵커라, 검색 레인의 '키워드를 제목에 넣어라'는 여기 적용하지 않는다
//   (씌우면 좋은 제목이 대량으로 죽는다 — 2026-08-02 실측). 가져오는 건 어미와 앞머리 수식절뿐이다.
export const TITLE_SHAPE_RULE = [
  "★제목 모양(유형과 무관하게 항상):",
  "  ① 앞에 수식절을 세운다 — 감정 판정(충격적인·씁쓸한·아찔한) / 반전 대비(~인데도·~해도) / 기간·횟수(5년 동안·11주 만에) / 뒤늦음(몰라도 너무 몰랐던) 중 하나.",
  "  ② 끝은 명사구 또는 전언형으로 끊는다 — '~다는 현실', '~하는 사람 특징', '~한 이유', '~ 기준', '~ 경우의 수', '~해도 될까?'.",
  "  ③ ★'~습니다/~됩니다/~입니다'로 문장을 닫지 마라. 닫힌 문장은 답처럼 읽혀 들어올 이유가 줄어든다. 답의 방향만 가리키고 끊는다.",
  "  ④ 사적 경험 서술 금지(경험형 제외) — 대신 전언형 '~다는/~라는'으로 쓰면 '사람들·데이터가 말하는 것'이 되어 날조가 아니면서 같은 후킹이 산다.",
  "  실측 결 예: '은근히 상대적 박탈감 느낀다는 40대 순자산 현실' / '월 실수령 350만원인데 재테크 못하는 사람 특징' / '20억 자가에 사는데도 하우스푸어라고 씁쓸하다는 중산층 기준'",
].join("\n");

// ★발행 제목 공식(2026-08-03) — 카드 제목과 '실제 글 제목'은 만드는 곳이 다르다.
//  유저 실측에서 검거: 카드 제목엔 공식을 넣었는데 발행 제목은 generateArticle이 따로 만들고 있었고,
//  그 지시가 "핵심 키워드를 앞쪽에, 검색 의도에 맞게"뿐이라 '신생아 전세대출 조건, …공제받는 방법'이 나왔다.
//  ★수식절도 여운 꼬리도 없이 답까지 다 준 제목이다 — 상위 44개와 정반대다.
//  ★한 곳에 두고 세 경로가 같이 읽는다: 검색 레인·홈판·발행 제목이 따로 놀면 반드시 드리프트한다.
export const PUBLISH_TITLE_FORMULA = [
  "★제목 공식(네이버 상위 경제 블로그 제목 44개 실측):",
  "  [수식절] + [핵심 키워드 명사구] + [여운 꼬리]",
  "  ① 앞에 수식절 — 감정 판정(충격적인·씁쓸한·아찔한) / 반전 대비(~인데도·~해도·평균 훌쩍 넘는데) / 기간·횟수(5년 동안·11주 만에) / 뒤늦음(몰라도 너무 몰랐던) 중 하나.",
  "  ② 핵심 키워드는 훼손하지 말고 그대로 둔다 — 검색 노출은 키워드가 가져온다.",
  "  ③ 끝은 명사구나 전언형으로 끊는다: '~다는 현실', '~하는 사람 특징', '~한 이유', '~ 기준', '~ 경우의 수', '~해도 될까?'.",
  "  ★완결 금지 — '~하는 방법', '~ 정리', '~ 총정리', '~ 알아야 할 것', '~ 확인하는 법'처럼 답까지 주면 클릭할 이유가 없다.",
  "  ★'~습니다/~됩니다/~입니다'로 문장을 닫지 마라.",
  "  ★사적 경험 서술 금지 — 대신 전언형('~다는/~라는')을 쓰면 '데이터·사람들이 말하는 것'이 되어 날조가 아니면서 후킹이 산다.",
  "  통과 예: '은근히 상대적 박탈감 느낀다는 40대 순자산 현실' / '월 실수령 350만원인데 재테크 못하는 사람 특징' / '따로 사는 무주택 부부도 된다는 신생아 전세대출 공제 조건'",
  "  실격 예: '신생아 전세대출 조건, 따로 사는 무주택 부부도 공제받는 방법'(수식절 없음·답까지 줌·'방법'으로 닫힘)",
].join("\n");

export const TITLE_TYPES: TitleType[] = [
  {
    key: "gain",
    name: "이득형",
    guide: "독자가 '받을 수 있는 것'을 앞세운다. 금액·대상이 구체적일수록 강하다. 단 '누구나 받는다'로 단정하지 말고 조건이 있다는 사실은 남긴다.",
    payoff: "본문에 실제 금액·대상 조건·받는 경로가 있어야 한다.",
    example: "안 찾아가면 그대로 사라진다는 평균 12만 원 미수령 환급금",
  },
  {
    key: "threat",
    name: "위협형",
    guide: "그대로 두면 '확정되는 손해'를 지적한다. ★공포 조장이 아니라 담담한 사실 통보다 — 겁주는 문장이 아니라 놓치면 실제로 사라지는 것을 말한다. 실제 제도 일정·기한이 있을 때만 쓴다.",
    payoff: "본문에 실제 기한·손해의 크기·막는 방법이 있어야 한다.",
    example: "이번 달 넘기면 자동 해지된다는, 놓치기 쉬운 신청 하나",
  },
  {
    key: "curious",
    name: "궁금형",
    guide: "답의 '존재'만 알리고 답은 숨긴다. 구체 숫자로 신뢰를 주되 그 숫자가 무엇을 뜻하는지는 본문에서. 통계·평균·순위처럼 독자가 자기 위치를 확인하고 싶어지는 소재에 특히 강하다.",
    payoff: "본문 앞부분에서 숨긴 답을 반드시 공개해야 한다(끝까지 미루면 이탈).",
    example: "생각보다 낮아서 씁쓸하다는 30대 평균 저축액",
  },
  {
    key: "compare",
    name: "비교형",
    guide: "두 선택지를 나란히 세우고 승자는 본문에서 밝힌다. 양쪽 나열로 끝내지 말고 '어떤 조건이면 어느 쪽'이라는 기준까지 간다.",
    payoff: "본문에 비교표 또는 조건별 갈림 기준이 있어야 한다.",
    example: "순서 틀리면 손해라는 연금저축과 IRP, 먼저 채울 쪽",
  },
  {
    key: "twist",
    name: "반전형",
    guide: "다들 믿는 상식이 사실과 다른 지점을 연다. ★단정형 조건부 허용(2026-08-03 개정): 본문이 공식 수치·제도 조항으로 뒷받침할 수 있으면 'A는 사실 B다'처럼 분명하게 써도 된다 — 종전엔 일괄 금지였는데 그러면 신선함이 죽는다. ★근거를 못 대면 종전대로 '~인 경우', '~할 수 있는 이유'처럼 조건형으로 연다. 기준은 단순하다: 본문에 출처 있는 숫자·조항이 있으면 단정, 없으면 조건.",
    payoff: "본문에 통념과 사실을 대조하는 근거(수치·제도 조항)가 있어야 한다.",
    example: "금리 높은 통장이 오히려 손해가 된다는 의외의 조건",
  },
  {
    key: "action",
    name: "실행형",
    guide: "오늘 할 수 있는 행동 하나를 숫자로 못 박는다. '알아보기·정리'가 아니라 '무엇을 어디서 몇 분이면'까지. 신청·조회·발급처럼 절차가 있는 글감에 맞다.",
    payoff: "본문에 순서(번호 또는 체크박스)와 소요 시간·경로가 있어야 한다.",
    example: "미루면 사라진다는 환급금, 5분이면 끝나는 조회 순서",
  },
  {
    key: "experience",
    name: "경험형",
    guide: "★운영자가 실제로 겪은 일이 입력됐을 때만 쓴다. 그 경험을 제목의 문을 여는 자리에 놓고, 본문이 그 경험에서 얻은 판단으로 이어지게 한다. 경험이 없으면 이 유형은 후보에서 제외된다(지어내면 계정이 죽는다).",
    payoff: "본문에 그 실경험이 재료로 들어가야 한다(입력된 범위 안에서만).",
    example: "작년에 30만 원 토해내고 알게 된 연말정산 함정",
  },
  // ★논쟁형 신설(2026-08-03 유저 확정 — 홈피드 전략 문서 반영).
  //  근거: 홈피드 노출 점수의 핵심이 체류·댓글인데, 댓글을 만드는 가장 강한 장치가 논쟁이다.
  //  우리 유형 7종에는 이 축이 통째로 없었다(이득·위협·궁금·비교·반전·실행·경험).
  //  ★단 '한쪽 편을 든다'를 아무 데나 쓰면 안 된다 — 유저 3원칙이 법적안전 > 노출극대 > 계정지속 순서다.
  //   재테크에서 확실한 주장은 투자 권유(금소법)가 될 수 있고, 의료·정치는 계정 리스크다.
  //   그래서 축을 코드로 제한한다(fitScore의 unsafeDebate) — 프롬프트로만 두면 반드시 새어 나간다.
  {
    key: "debate",
    name: "논쟁형",
    guide: "사람들 의견이 실제로 갈리는 지점을 열고, 한쪽 입장을 분명히 밝힌다. ★양비론 금지 — '상황에 따라 다릅니다'로 끝내면 이 유형을 쓰는 의미가 없다. 대신 반대편 논리를 먼저 공정하게 요약한 뒤 내 판단을 말한다(무시하고 단정하면 반박이 아니라 조롱을 부른다). ★쓸 수 있는 축: 제도의 실효성, 소비·저축 습관, 세금 구조처럼 '판단이 갈리지만 사실로 따질 수 있는 것'.",
    payoff: "본문에 ①반대편 논리 요약 ②내 판단과 그 근거(공식 수치·제도 조항) ③독자가 자기 상황을 대입할 기준이 있어야 한다.",
    example: "5년 묶이는 게 정말 이득일까, 청년도약계좌를 말리는 이유",
  },
];

export const TITLE_TYPE_BY_KEY: Record<TitleTypeKey, TitleType> =
  Object.fromEntries(TITLE_TYPES.map((t) => [t.key, t])) as Record<TitleTypeKey, TitleType>;

/** 글감에서 읽어낸 신호 — 적합도의 입력. 텍스트에서 자동 추출하거나 호출측이 직접 준다. */
export interface TopicSignals {
  /** 실제 기한·마감·시행일이 있는가 */
  deadline: boolean;
  /** ★받을 수 있는 돈 신호의 '밀도'(불리언이 아니라 개수) — 동점 때 순서로 갈리지 않게 한다 */
  benefitHits: number;
  /** 비교 축이 둘 이상인가 */
  compare: boolean;
  /** 통념·오해를 뒤집는 소재인가 */
  myth: boolean;
  /** ★절차 신호의 밀도(신청·조회·방법·순서…) */
  procedureHits: number;
  /** 통계·평균·순위 등 자기 위치 확인 소재인가 */
  statistic: boolean;
  /** 구체 숫자·금액이 자료에 있는가 */
  hasNumber: boolean;
  /** 운영자 실경험이 입력됐는가 — experience 유형의 전제조건 */
  userExperience: boolean;
  /** ★의견이 갈리는 축인가(제도 실효성·습관·구조 선택) — debate 유형의 전제조건 */
  debatable: boolean;
  /** ★논쟁을 열면 안 되는 소재인가(투자 권유·의료·정치) — 있으면 debate는 후보에서 제외된다 */
  unsafeDebate: boolean;
}

// ★밀도를 세는 축(benefit·procedure)은 g 플래그로 둔다 — 불리언만 보면
//  '할인 신청 방법 순서 준비물'(절차 6신호)과 '할인'(이득 1신호)이 같은 점수가 되어
//  유형이 배열 순서로 갈린다(실측: 절차형 글감에 이득형이 걸렸다).
const RE = {
  deadline: /(마감|기한|까지|D-\d|\d{1,2}월\s?\d{1,2}일|종료|시행|접수\s*기간|신청\s*기간|자동\s*해지|연장)/,
  benefit: /(지원금|보조금|환급|미환급|바우처|공제|혜택|포인트|장려금|수당|보험금|캐시백|할인)/g,
  compare: /(비교|vs|VS|대비|차이|어느\s*쪽|둘\s*중|보다\s*유리|와\s*\S+\s*중)/,
  myth: /(오해|착각|사실은|알고\s*보면|의외|통념|잘못\s*알|다들\s*믿|함정|아닙니다|반전)/,
  procedure: /(신청|조회|발급|접수|가입|해지|이전|등록|제출|절차|방법|순서|준비물)/g,
  statistic: /(평균|중위|비율|순위|통계|얼마나|몇\s*%|퍼센트|상위\s*\d|하위\s*\d|격차)/,
  hasNumber: /\d[\d,]*\s*(원|만\s?원|억|%|퍼센트|년|개월|일|위|배)/,
  // ★논쟁 가능 축 — '판단이 갈리지만 사실로 따질 수 있는 것'(제도 실효성·습관·구조 선택)
  // ★좁게 잡는다(2026-08-03 회귀에서 검거) — 처음엔 '유리·손해·추천·가입'까지 넣었더니
  //  재테크 글감엔 그 단어가 거의 항상 있어서 논쟁형이 비교형 자리를 뺏었다.
  //  ★진짜 논쟁 표지만 남긴다: '의견이 갈린다'는 신호가 텍스트에 실제로 있어야 연다.
  //  없는 논쟁을 만들어 내는 게 곧 어그로다(문서가 말한 '어그로와 전략의 차이').
  debatable: /(과연|굳이|필요\s*없|무용|비추|말리|논란|갈린|반대\s*의견|이득일까|손해일까|해야\s*할까|맞을까|괜찮을까|정말\s*(이득|손해|필요|좋))/,
  // ★논쟁을 열면 안 되는 소재(2026-08-03). 유저 3원칙: 법적안전 > 노출극대 > 계정지속.
  //  ⓐ투자 권유(금소법) ⓑ의료·건강 단정 ⓒ정치. 이 셋은 노출을 얻는 대신 계정을 건다.
  //  ★프롬프트가 아니라 여기서 막는다 — '한쪽 편을 들어라'는 지시와 '이건 빼라'는 지시를
  //   같은 프롬프트에 넣으면 모델이 반드시 한쪽을 흘린다(CLAUDE.md: 프롬프트는 방향, 코드는 한계선).
  unsafeDebate: /(종목|주식|코인|비트코인|매수|매도|수익률|급등|테마주|상장|공모주|투자\s*(추천|해도|하라)|사도\s*될|팔아야|병원|질환|치료|증상|처방|의사|한의|영양제|다이어트\s*약|탄핵|선거|정당|대통령|후보)/,
};

const hits = (t: string, re: RegExp): number => t.match(re)?.length ?? 0;

/** 글감 텍스트(키워드+각도+뉴스 발췌 등)에서 신호를 읽는다. */
export function readSignals(text: string, opts: { userExperience?: boolean } = {}): TopicSignals {
  const t = String(text || "");
  return {
    deadline: RE.deadline.test(t),
    benefitHits: hits(t, RE.benefit),
    compare: RE.compare.test(t),
    myth: RE.myth.test(t),
    procedureHits: hits(t, RE.procedure),
    statistic: RE.statistic.test(t),
    hasNumber: RE.hasNumber.test(t),
    userExperience: Boolean(opts.userExperience),
    debatable: RE.debatable.test(t),
    unsafeDebate: RE.unsafeDebate.test(t),
  };
}

/**
 * 유형별 적합도 점수. 높을수록 이 글감에 맞는 제목 유형이다.
 * ★경험형은 실경험이 없으면 후보에서 완전히 빠진다(-1) — 지어낸 경험은 이 시스템의 금지선이다.
 * ★위협형도 실제 기한이 없으면 빠진다(-1) — 기한 없는 위협은 정보가 아니라 공포 조장이다.
 */
export function fitScore(key: TitleTypeKey, s: TopicSignals): number {
  switch (key) {
    case "gain":
      // 이득 신호가 여러 개 겹칠수록(환급+지원금+혜택) 이 글의 본질이 '받는 돈'이다.
      return s.benefitHits > 0 ? 3 + Math.min(s.benefitHits - 1, 1) + (s.hasNumber ? 1 : 0) : 0;
    case "threat":
      return s.deadline ? 4 + (s.benefitHits > 0 ? 1 : 0) : -1;
    case "curious":
      // 어느 글감에나 성립하는 기본형 — 1점을 깔아 두고 통계 소재면 강해진다.
      return 1 + (s.statistic ? 2 : 0) + (s.hasNumber ? 1 : 0);
    case "compare":
      return s.compare ? 3 + (s.hasNumber ? 1 : 0) : -1;
    case "twist":
      return s.myth ? 4 : 0;
    case "action":
      // 절차 신호가 촘촘한 글은 '오늘 뭘 하면 되는지'가 본질이다.
      return s.procedureHits > 0 ? 3 + Math.min(s.procedureHits - 1, 2) : 0;
    case "experience":
      return s.userExperience ? 6 : -1;
    case "debate":
      // ★위험축이면 후보에서 완전히 제외(-1). 노출을 얻는 대신 계정을 거는 거래는 하지 않는다.
      //  안전축이어도 '갈리는 지점'이 실제로 있어야 연다 — 없는 논쟁을 만들면 그게 어그로다.
      if (s.unsafeDebate) return -1;
      // ★기본 3점 — 비교축이 뚜렷한 글감(compare 3+1)에는 비교형을 양보한다.
      //  논쟁형은 '비교로 안 풀리는, 입장이 갈리는 것'을 맡는다.
      return s.debatable ? 3 + (s.hasNumber ? 1 : 0) : -1;
  }
}

/**
 * 이 글감에 가장 맞는 제목 유형을 고른다.
 * 동점이면 최근에 안 쓴 유형을 택한다(연속 반복 방지 — 추첨은 여기서만 쓴다).
 * 후보가 전부 음수면 궁금형으로 떨어진다(항상 성립하는 기본형).
 */
export function pickTitleType(signals: TopicSignals, recentKeys: string[] = []): TitleType {
  const recent = new Set(recentKeys.slice(0, 3));
  const scored = TITLE_TYPES.map((t) => ({ t, score: fitScore(t.key, signals) })).filter((x) => x.score > 0);
  if (scored.length === 0) return TITLE_TYPE_BY_KEY.curious;
  const best = Math.max(...scored.map((x) => x.score));
  const top = scored.filter((x) => x.score === best);
  const fresh = top.filter((x) => !recent.has(x.t.key));
  return (fresh.length ? fresh : top)[0]!.t;
}

/** 프롬프트에 넣을 지시 블록 — 유형 하나를 지정하고 그 약속까지 함께 준다. */
export function titleTypeDirective(t: TitleType): string {
  return [
    `★제목 유형: [${t.name}] — ${t.guide}`,
    // ★모양 규칙을 유형 지시와 함께 내려보낸다(2026-08-03) — 유형만 주면 '무엇을 약속할지'는
    //  정해지지만 '어떻게 끝맺을지'가 안 정해져서 매번 '~습니다'로 닫힌 문장이 나왔다.
    TITLE_SHAPE_RULE,
    `  결 예시(그대로 베끼지 말 것): "${t.example}"`,
    `  ★본문 이행 의무: ${t.payoff} 제목이 약속하고 본문이 안 지키면 낚시이고, 홈판에서 낚시는 이탈률로 즉시 되돌아온다.`,
  ].join("\n");
}
