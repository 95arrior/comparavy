// 네이버 발행 복사 모드 설정. 스마트에디터ONE의 외부 이미지 자동 업로드 여부는 수동 실측 대상이라
// 두 모드를 모두 구현하고 이 상수 하나로 전환한다.
export const PASTE_MODE: "rich" | "marker" = "rich"; // 실측 후 확정

// 네이버 글쓰기 진입 기준 URL(데스크톱). 모바일은 lib/naverApp의 앱 우선 열기를 쓴다.
export const NAVER_WRITE_URL = "https://blog.naver.com";

// 본문 정렬 — ★실기기 모바일 관찰 기반 최종 결정: 왼쪽 정렬로 통일(포맷 챕터 동결).
//  본문·소제목·데이터줄·리스트 전부 왼쪽 단일 세계. 중앙/왼쪽 혼용·데이터박스 폐기.
//  변경은 실사용 데이터(체류·이탈) 근거 있을 때만 재론.
export const BODY_ALIGN: "center" | "left" = "center"; // ★2026-07-07 유저 확정 — 40/50대 모바일 독자, 중앙 정렬+짧은 리듬

// ★AI 이미지 재개(유저 결정 2026-07-05) — 검토 화면 슬롯별 생성 버튼(✦·크레딧 표기)로 부활.
//  해제 조건이던 스타일 다양화는 시드 회전(톤5×구도5×무드5)으로 충족. 슬롯 인덱스 문서순 단일화 완료.
export const AI_IMAGES_ENABLED = true;

// 글 생성 시트의 '이미지 동시 생성' 토글 — 계속 오프(생성 흐름은 침묵·심플 유지, 이미지는 검토 화면에서).
export const AI_IMAGES_AT_CREATE = false;

// ★데이터 카드 봉인(최종 결정 2026-07-04) — 생성 이미지 전면 제거. 코드 보존, 플래그 오프.
//  수치는 '데이터 줄 묶음'(텍스트), 행동 순서는 체크리스트가 맡는다. 모든 슬롯 = 업로드형.
export const DATA_CARDS_ENABLED = false;
