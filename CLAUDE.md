# AteFlo 개발 원칙

## 상태 저장 원칙 (2026-07-08 상태 전수 조사 후 확정)

1. **블로그 단위로 달라질 수 있는 상태는 저장 키에 반드시 blogId(profileKey)를 포함한다. 날짜만으로 키를 만들지 않는다.**
   - profileKey 형식: `{blogId}:{vertical}:{sub_category}`
   - 날짜 키는 로컬(KST) 기준 — `toISOString()`(UTC)은 자정~오전 9시에 '어제'로 계산되는 버그를 만든다.

2. **키 스코프 변경 시 이관/폐기 판단 원칙:**
   - 목록(누적형: 치운 글감, 스킵 목록)은 활성 블로그로 이관
   - 카운터·낙관 플래그(휘발형: 교체 횟수, 발행 플래그)는 폐기 — 관대한 쪽 오류가 낫다
   - 영구 플래그(승인 상태)는 값이 있을 때만 활성 블로그로 조건부 이관(비파괴 — 잘못 귀속 시 재입력으로 복구 가능해야 한다)

3. **글 삭제 핸들러는 해당 글의 로컬 파생 데이터를 함께 제거한다.**
   - 대상: `ateflo_imgs_{id}`, `ateflo_thumb_{id}`, `ateflo_tcopy_{id}` (이미지·썸네일·문구 캐시)

## 기타 확정 원칙
- 프론트 정규식은 Safari(JavaScriptCore) 기준 — 가변 길이 lookbehind 금지(파싱 SyntaxError로 화면 전체 사망).
- 상태 A를 바꾼 직후 상태 A를 읽는 코드 금지 — 바꾼 값을 변수로 직접 넘긴다(ref 갱신 타이밍 사고 3회 실측).
- 프롬프트는 방향, 코드는 한계선 — 모델이 어길 수 있는 규칙(형광펜 총량 등)은 코드 게이트로 강제한다.
- 사이트의 어떤 텍스트도 자동화를 광고하지 않는다(구글·애드센스 앞 금기).

## 게이트 중앙화 원칙 (2026-07-09 — 반복 실수 3형 매듭)
- **글감 검문 규칙은 lib/cardFinalGate.ts 한 곳에만 추가한다.** 소스별(수확기별)로 게이트를 복붙하면 반드시 빠지는 경로가 생긴다(실측: 구·군 게이트가 수확기 2곳에만 있어 뉴스 증식 경로가 무검문).
- 게이트는 특정 표기(태그·마커 리터럴)가 아니라 **결과(배경색·의미)를 검사**한다 — 표기는 모델이 계속 바꾼다(실측: mark→b style 형광 우회).
- **후보 목록을 필터하면 그 목록의 파생 참조(짝짓기·인덱스)를 전부 같이 바꾼다** — 절반만 바꾸면 짝이 밀린다(실측: 제목-키워드 어긋남).
- 새 게이트에는 scripts/check-final-gate.mjs에 실측 케이스를 추가한다 — 유저가 잡은 실물이 곧 영구 회귀 테스트다.

- 본문 정렬 최종(2026-07-10 유저 A/B 실측, 2026-07-17 v6 개정): **중앙 정렬 + 개행 v6** — 한 줄 '띄어쓰기 포함 18자' 상한, 꼬리줄 5자 미만 금지(달랑 1~2자 줄 = 흉함), 의미 경계 우선·어절 폴백·단어 중간 절단 금지·URL 통줄. 리스트·표만 좌. 좌측 문서체 실험은 실측으로 기각 — 재제안 금지. 분량은 네이버 1,200~1,800(18자 개행에서 긴 글=도배), **하드 상한 2,500자**(2026-08-04 유저 확정 — 넘으면 코드가 섹션을 잘라낸다. 경고·재생성은 실패할 수 있어 실행으로 바꿨다), 소제목은 색상이 아니라 세로 바(버티컬 라인)로 구분.
